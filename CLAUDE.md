# CLAUDE.md

本文件为 Claude Code 在本仓库工作的约定。

## 代码复审

当我完成涉及以下任一情况的改动后，自动用 codex 工具以 read-only 方式复审，
再把改动连同 codex 的复审结论一起交给我，不用我额外要求：

- 主题 token / CSS 变量（明暗两套值）
- 跨多个文件的重构
- Mira 视觉相关（companion.html、动画、眼睛颜色等）
- 任何动到资金/链上逻辑的（以后 intent-swap 用得上）

复审只读不改。codex 报的问题按严重度列出，你不要替它过滤掉，原样给我看。
日常小改动（单文件、文案、注释）不必复审，直接给我。
